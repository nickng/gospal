def main.main():
    let t2 = newchan main.main0.t2_chan1, 1;
    call main.Send(t2);
    call main.Recv(t2);
    call main.Recv(t2);
    spawn main.Send(t2);
    spawn main.Recv(t2);
    call main.Send(t2);
    call main.Recv(t2);
def main.Send(sr_0):
    send sr_0;
def main.Recv(sr_0):
    recv sr_0;
