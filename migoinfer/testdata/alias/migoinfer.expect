def main.main():
    let t0 = newchan main.main0.t0_chan1, 1;
    call main.sendAlias(t0, t0);
    let t2 = newchan main.main0.t2_chan1, 1;
    call main.sendAlias(t0, t2);
    recv t0;
def main.sendAlias(ch1, ch2):
    send ch2;
